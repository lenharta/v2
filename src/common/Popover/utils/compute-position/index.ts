// export function computePosition(target: unknown, floating: unknown, config: {}) {}

import { ComputePosition } from '../../types';

export interface ElementRectsInput {
  floating: any;
  target: any;
}

export const getElementRects = async (props: ElementRectsInput) => {};

export const computePosition = async () => {};
