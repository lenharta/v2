export const TOP_LAYER_SELECTORS = [':popover-open', ':modal'] as const;

export const INVERSE_SIDE_MAP = { left: 'right', right: 'left', bottom: 'top', top: 'bottom' };

export const INVERSE_ALIGNMENT_MAP = { start: 'end', end: 'start' };
