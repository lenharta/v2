export const ControlIndicator = (props: { style?: React.CSSProperties }) => {
  const { style } = props;
  return <div className="Control-indicator" style={style} />;
};

ControlIndicator.displayName = '@v2/Control.Indicator';
