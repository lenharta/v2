export const isHovered = (hovered?: boolean) => hovered === true;

export const isReadOnly = (readOnly?: boolean) => readOnly === true;

export const isDisabled = (disabled?: boolean) => disabled === true;
