import { Theme } from '@/types';

interface V2LogoProps {
  size?: Theme.SizeExpanded | undefined;
  scheme?: Theme.Scheme | undefined;
  variant?: 'default' | 'elevated' | undefined;
}

export type { V2LogoProps };
