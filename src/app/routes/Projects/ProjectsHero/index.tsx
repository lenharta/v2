import { Hero } from '@/app/components';
import { Title } from '@/core';

export const ProjectsHero: React.FC = () => {
  return (
    <Hero>
      <Title>Projects</Title>
    </Hero>
  );
};

ProjectsHero.displayName = '@v2/route/Projects.Hero';
