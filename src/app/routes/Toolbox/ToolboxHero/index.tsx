import { Hero } from '@/app/components';
import { Title } from '@/core';

export const ToolboxHero: React.FC = () => {
  return (
    <Hero>
      <Title>Toolbox</Title>
    </Hero>
  );
};

ToolboxHero.displayName = '@v2/route/Toolbox.Hero';
