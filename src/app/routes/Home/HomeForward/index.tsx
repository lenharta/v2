import { Section } from '@/app/common';
import { Box, Text, Title } from '@/core';

export const HomeForwardHero = () => {
  return (
    <Section className="sec-home-forward-hero">
      <Box className="sec-home-forward-hero-box">
        <Title>Crafting Digital Dreams for businesses and brands to move society forward.</Title>
      </Box>
      <Box className="sec-home-forward-hero-box"></Box>
    </Section>
  );
};
