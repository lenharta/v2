import { Hero } from '@/app/components';
import { Title } from '@/core';

export const PreferencesHero: React.FC = () => {
  return (
    <Hero>
      <Title>Preferences</Title>
    </Hero>
  );
};

PreferencesHero.displayName = '@v2/route/Preferences.Hero';
