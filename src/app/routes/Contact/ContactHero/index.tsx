import { Hero } from '@/app/components';
import { Title } from '@/core';

export const ContactHero: React.FC = () => {
  return (
    <Hero>
      <Title>Contact</Title>
    </Hero>
  );
};

ContactHero.displayName = '@v2/route/Contact.Hero';
