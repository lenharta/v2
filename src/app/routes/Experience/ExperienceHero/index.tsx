import { Hero } from '@/app/components';
import { Title } from '@/core';

export const ExperienceHero: React.FC = () => {
  return (
    <Hero>
      <Title>Experience</Title>
    </Hero>
  );
};

ExperienceHero.displayName = '@v2/route/Experience.Hero';
